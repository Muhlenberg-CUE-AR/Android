/*package muhlenberg.edu.cue;

import android.content.Context;
import android.hardware.Camera;
import android.os.Build;

import org.junit.Test;

import static android.hardware.camera2.CameraCharacteristics.SENSOR_INFO_PHYSICAL_SIZE;
import static android.hardware.camera2.CaptureResult.LENS_FOCAL_LENGTH;

/**
 * Created by Willy on 2/9/2017.
 * Jalal is lame
 */
/*
public class SensorServiceTest {

    @Test
    public void testFieldOfView() throws Exception {
        if (Build.VERSION.SDK_INT >= 21) {
            float FOVx = 2 * atan(SENSOR_INFO_PHYSICAL_SIZE.x / (2.0 * LENS_FOCAL_LENGTH));
            float FOVy = 2 * atan(SENSOR_INFO_PHYSICAL_SIZE.y / (2.0 * LENS_FOCAL_LENGTH));
        }
        else {
            Camera.Parameters.
        }
    }
}
*/